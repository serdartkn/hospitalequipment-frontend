import { FilterClinicPipe } from './filter-clinic.pipe';

describe('FilterClinicPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterClinicPipe();
    expect(pipe).toBeTruthy();
  });
});
